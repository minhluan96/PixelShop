@using PixelShop.Models

<div class="container" style="width:100%; position:relative;">
    <div class="col-md-2 logo-w3">
        <a href="index.html"><img src="~/images/logo.png" alt="logo pixel" /></a>
    </div>

    <div class="col-md-7 navigation-agileits">
        <nav class="navbar navbar-default">
            <div class="navbar-header nav_2">
                <button type="button" class="navbar-toggle collapsed navbar-toggle1" data-toggle="collapse" data-target="#bs-megadropdown-tabs">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            <div class="collapse navbar-collapse" id="bs-megadropdown-tabs">
                <ul class="nav navbar-nav ">
                    <li class="active"><a href="/Home" class="hyper"><span>Trang chủ</span></a></li>
                    <li class="dropdown ">
                        <a href="@Url.Action("DanhMuc", "TimKiem")" class="dropdown-toggle  hyper" data-toggle="dropdown"><span>Đồng hồ<b class="caret"></b></span></a>
                        <ul class="dropdown-menu multi">
                            <div class="row">
                                @{ List<DANHMUC> lstDM = ViewData["danhmuc"] as List<DANHMUC>; }
                                <div class="col-sm-4">
                                    <ul class="multi-column-dropdown">
                                        @for (int i = 0; i < lstDM.Count(); i++)
                                        {
                                            <li><a href="@Url.Action("DanhMuc", "TimKiem", new { id = lstDM.ElementAt(i).MaDanhMuc})">@Html.DisplayFor(item => lstDM.ElementAt(i).TenDanhMuc)</a></li>
                                        }
                                    </ul>

                                </div>
                                <div class="col-sm-4">
                                    <ul class="multi-column-dropdown">
                                        <li><a href="casuals.html">Đồng hồ thể thao</a></li>
                                        <li><a href="night.html">Đồng hồ hiện đại</a></li>
                                        <li><a href="formals.html">Đồng hồ điện tử</a></li>
                                        <li><a href="inner.html">Đồng hồ thông minh</a></li>

                                    </ul>
                                </div>
                                <div class="col-sm-4 w3l">
                                    <a href="women.html"><img src="~/images/back5.jpg" class="img-responsive" alt=""></a>
                                </div>
                                <div class="clearfix"></div>
                            </div>
                        </ul>
                    </li>
                    <li class="dropdown">
                        <a href="@Url.Action("NhaSanXuat", "TimKiem")" class="dropdown-toggle hyper" data-toggle="dropdown"><span> Hãng sản xuất <b class="caret"></b></span></a>
                        <ul class="dropdown-menu multi">
                            <div class="row">
                                @{ List<NHASANXUAT> lstNSX = ViewData["nhasanxuat"] as List<NHASANXUAT>; }
                                <div class="col-sm-4">
                                    <ul class="multi-column-dropdown">
                                        @for (int i = 0; i < lstNSX.Count() / 2; i++)
                                        {
                                            <li><a href="@Url.Action("NhaSanXuat", "TimKiem", new { id = lstNSX.ElementAt(i).MaNSX})">@Html.DisplayFor(item => lstNSX.ElementAt(i).TenNSX)</a></li>
                                        }
                                    </ul>

                                </div>
                                <div class="col-sm-4">

                                    <ul class="multi-column-dropdown">
                                        @for (int i = lstNSX.Count() / 2; i < lstNSX.Count(); i++)
                                        {
                                            <li><a href="@Url.Action("NhaSanXuat", "TimKiem", new { id = lstNSX.ElementAt(i).MaNSX})">@Html.DisplayFor(item => lstNSX.ElementAt(i).TenNSX)</a></li>
                                        }

                                    </ul>

                                </div>
                                <div class="col-sm-4 w3l">
                                    <a href="jewellery.html"><img src="/images/back6.jpg" class="img-responsive" alt=""></a>
                                </div>
                                <div class="clearfix"></div>
                            </div>
                        </ul>
                    </li>
                    <li><a href="/About" class="hyper"><span>Giới thiệu</span></a></li>
                    <li><a href="contact.html" class="hyper"><span>Liên hệ</span></a></li>
                </ul>
            </div>
        </nav>
    </div>
    <div class="col-md-3 search-agileinfo pull-right">
       
            @using (Html.BeginForm("Index", "TimKiem"))
            {
            <input type="search" name="Search" placeholder="Tìm kiếm" required=""/>
            <button type="submit" class="btn btn-default search" aria-label="Left Align">
                <i class="fa fa-search" aria-hidden="true"> </i>
            </button>
            }
        
    </div>
    <div class="clearfix"></div>
</div>
